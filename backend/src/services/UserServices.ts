import sqlAccess from "../data/SQLAccess";

const registerNewUser = (expressRequest, expressResponse, next) => {

};

const loginUser = (expressRequest, expressResponse, next) => {

};

const changeUserPassword = (expressRequest, expressResponse, next) => {

};

const resetForgottenPassword = (expressRequest, expressResponse, next) => {

};

const sendResetPasswordMail = (expressRequest, expressResponse, next) => {

};

const confirmUserAccount = (expressRequest, expressResponse, next) => {

};

export {
    registerNewUser,
    loginUser,
    changeUserPassword,
    resetForgottenPassword,
    sendResetPasswordMail,
    confirmUserAccount
}
